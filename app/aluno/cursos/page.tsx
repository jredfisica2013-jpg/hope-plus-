'use client'
import {useMemo,useState} from 'react'
import {Search,SlidersHorizontal} from 'lucide-react'
import {CourseCard,MemberShell,courses} from '../components'
export default function Cursos(){const[q,setQ]=useState('');const[f,setF]=useState('Todos');const list=useMemo(()=>courses.filter(c=>(f==='Todos'||(f==='Concluídos'&&c.progress===100)||(f==='Em andamento'&&c.progress>0&&c.progress<100)||(f==='Novos'&&c.progress===0))&&(c.title+' '+c.tag+' '+c.desc).toLowerCase().includes(q.toLowerCase())),[q,f]);return <MemberShell active="/aluno/cursos" title="Cursos"><div className="mx-auto max-w-[1500px] px-4 pb-28 pt-7 sm:px-6 lg:px-10 lg:pb-12 lg:pt-10"><div className="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-[9px] font-black uppercase tracking-[.25em] text-[#555]">Biblioteca de treinamento</p><h1 className="mt-2 text-4xl font-black tracking-[-.04em]">Cursos</h1><p className="mt-2 max-w-xl text-xs leading-5 text-[#666] sm:text-sm">Conteúdo organizado para sair da intenção e entrar em execução.</p></div><div className="flex w-full items-center gap-3 rounded-2xl border border-white/[.07] bg-[#131313] px-4 py-3 lg:w-[360px]"><Search size={17} className="text-[#555]"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar treinamento..." className="w-full bg-transparent text-xs outline-none placeholder:text-[#555]"/><SlidersHorizontal size={15} className="text-[#555]"/></div></div><div className="mt-7 flex gap-2 overflow-x-auto pb-1">{['Todos','Em andamento','Concluídos','Novos'].map(x=><button key={x} onClick={()=>setF(x)} className={`shrink-0 rounded-full px-5 py-2.5 text-[9px] font-black transition ${f===x?'bg-[#FF6B00] text-white':'border border-white/[.07] bg-[#131313] text-[#666] hover:text-white'}`}>{x}</button>)}</div><div className="mt-7 grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{list.map(c=><CourseCard key={c.id} course={c}/>)}</div>{!list.length&&<div className="mt-10 rounded-2xl border border-dashed border-white/10 p-12 text-center"><p className="text-sm font-bold">Nenhum treinamento encontrado.</p><p className="mt-2 text-xs text-[#555]">Ajuste sua busca ou filtro.</p></div>}</div></MemberShell>}
