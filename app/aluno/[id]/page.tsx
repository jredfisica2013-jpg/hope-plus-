'use client'
import {useEffect,useMemo,useState} from 'react'
import {useParams} from 'next/navigation'
import {ArrowLeft,ArrowRight,CheckCircle2,Clock,Play,RotateCcw} from 'lucide-react'
import {courses,MemberShell} from '../components'
const lessons=['Introdução','O princípio da disciplina','Aplicação na vida real','Exercício prático','Próximo nível']
export default function Curso(){const{id}=useParams<{id:string}>();const base=useMemo(()=>courses.find(c=>c.id===id)||courses[0],[id]);const[lesson,setLesson]=useState(0);const[done,setDone]=useState<number[]>([]);useEffect(()=>{try{const x=JSON.parse(localStorage.getItem(`hope_lessons_${id}`)||'[]');if(Array.isArray(x))setDone(x)}catch{}},[id]);const toggle=()=>{const next=done.includes(lesson)?done.filter(x=>x!==lesson):[...done,lesson];setDone(next);localStorage.setItem(`hope_lessons_${id}`,JSON.stringify(next))};const pct=Math.min(100,Math.max(base.progress,Math.round(Math.max(base.progress,done.length*20))));return <MemberShell active="/aluno/cursos" title={base.title}><div className="mx-auto max-w-[1400px] px-4 pb-28 pt-5 sm:px-6 lg:px-10 lg:pb-12 lg:pt-7"><div className="mb-5 flex items-center justify-between"><a href="/aluno/cursos" className="inline-flex items-center gap-2 text-[10px] font-bold text-[#666] hover:text-white"><ArrowLeft size={15}/> VOLTAR PARA CURSOS</a><span className="text-[9px] font-black text-[#FF6B00]">{pct}% CONCLUÍDO</span></div><div className="grid gap-5 lg:grid-cols-[1fr_360px]"><section><div className="relative aspect-video overflow-hidden rounded-[24px] border border-white/[.07] bg-[#101010]"><img src={base.thumb} alt="" className="absolute inset-0 h-full w-full object-cover opacity-45"/><div className="absolute inset-0 bg-gradient-to-t from-black via-black/20 to-black/30"/><button onClick={toggle} aria-label="Marcar aula como concluída" className="absolute left-1/2 top-1/2 grid h-16 w-16 -translate-x-1/2 -translate-y-1/2 place-items-center rounded-full bg-[#FF6B00] shadow-[0_0_50px_rgba(255,107,0,.2)] transition hover:scale-105"><Play size={24} fill="white"/></button><div className="absolute bottom-4 left-4 right-4 flex items-end justify-between"><div><p className="text-[8px] font-black uppercase tracking-widest text-white/50">AULA {String(lesson+1).padStart(2,'0')}</p><h2 className="mt-1 text-sm font-black sm:text-lg">{lessons[lesson]}</h2></div><span className="rounded-full bg-black/60 px-3 py-1.5 text-[8px] font-bold text-white/60">{done.includes(lesson)?'CONCLUÍDA':'EM ANDAMENTO'}</span></div></div><div className="mt-6 flex items-start justify-between gap-4"><div><span className="rounded-md bg-[#FF6B00] px-2 py-1 text-[8px] font-black">{base.tag}</span><h1 className="mt-3 text-2xl font-black tracking-tight sm:text-3xl">{base.title}</h1><p className="mt-2 max-w-2xl text-xs leading-6 text-[#666]">{base.desc}</p></div><Clock className="hidden text-[#555] sm:block" size={20}/></div><div className="mt-6 flex gap-2"><button onClick={()=>setLesson(Math.max(0,lesson-1))} disabled={!lesson} className="inline-flex flex-1 items-center justify-center gap-2 rounded-xl border border-white/[.07] px-4 py-3 text-[9px] font-black disabled:opacity-30"><ArrowLeft size={13}/> ANTERIOR</button><button onClick={()=>setLesson(Math.min(lessons.length-1,lesson+1))} disabled={lesson===lessons.length-1} className="inline-flex flex-1 items-center justify-center gap-2 rounded-xl bg-[#FF6B00] px-4 py-3 text-[9px] font-black disabled:opacity-30">PRÓXIMA <ArrowRight size={13}/></button></div></section><aside className="h-fit overflow-hidden rounded-[24px] border border-white/[.07] bg-[#131313]"><div className="border-b border-white/[.06] p-5"><p className="text-[9px] font-black uppercase tracking-[.18em] text-[#555]">Conteúdo</p><h2 className="mt-1 font-black">Módulo 01 · Fundamentos</h2><div className="mt-4 h-1.5 rounded-full bg-white/[.06]"><div className="h-full rounded-full bg-[#FF6B00]" style={{width:`${pct}%`}}/></div></div><div className="p-2">{lessons.map((x,i)=><button key={x} onClick={()=>setLesson(i)} className={`flex w-full items-center gap-3 rounded-xl p-3 text-left transition ${lesson===i?'bg-[#FF6B00]/10':''}`}><span className={`grid h-8 w-8 shrink-0 place-items-center rounded-full ${done.includes(i)?'bg-[#FF6B00] text-white':'bg-white/[.04] text-[#555]'}`}>{done.includes(i)?<CheckCircle2 size={14}/>:<span className="text-[9px] font-black">{String(i+1).padStart(2,'0')}</span>}</span><span className={`flex-1 text-[10px] font-bold ${lesson===i?'text-white':'text-[#666]'}`}>{x}</span></button>)}</div><div className="border-t border-white/[.06] p-4"><button onClick={()=>{setDone([]);localStorage.removeItem(`hope_lessons_${id}`)}} className="inline-flex items-center gap-2 text-[8px] font-black text-[#444] hover:text-white"><RotateCcw size={12}/> RESETAR PROGRESSO LOCAL</button></div></aside></div></div></MemberShell>}
