'use client'
import {useEffect,useState} from 'react';import {useParams,useRouter} from 'next/navigation'
type C={id:string;tag:string;title:string;desc:string;thumb:string;video:string;progress:number}
const fallback:C={id:'',tag:'HOPE+',title:'Conteúdo HOPE+',desc:'Conteúdo de desenvolvimento e execução.',thumb:'/codigo.svg',video:'https://www.youtube.com/embed/dQw4w9WgXcQ',progress:0}
export default function Curso(){const {id}=useParams<{id:string}>();const r=useRouter();const [c,setC]=useState<C>(fallback);useEffect(()=>{try{const all=JSON.parse(localStorage.getItem('hope_content_v2')||'[]');setC(all.find((x:C)=>x.id===id)||fallback)}catch{}},[id]);return <main className="app detail"><header className="top"><button className="back" onClick={()=>r.push('/aluno')}>←</button><b className="brand">HOPE<span>+</span></b><div/></header><section className="detailContent"><div className="player"><iframe src={c.video} title={c.title} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen/></div><span className="tag">{c.tag}</span><h1>{c.title}</h1><div className="progress"><span style={{width:c.progress+'%'}}/></div><p className="completion">{c.progress}% concluído</p><div className="description"><h2>Sobre este conteúdo</h2><p>{c.desc}</p></div><button className="orangeBtn" onClick={()=>r.push('/aluno')}>← VOLTAR PARA CONTEÚDOS</button></section></main>}
