'use client'
import {useState} from 'react'
import Link from 'next/link'
import {ArrowLeft,Heart,MessageCircle,Send,Users} from 'lucide-react'
const posts=[['HOPE+','Disciplina é fazer o combinado mesmo quando a vontade muda.','Hoje'],['Comunidade','Qual foi a execução que você não queria fazer — mas fez?','Ontem']]
export default function Comunidade(){const [text,setText]=useState('');const [postsLocal,setPosts]=useState(posts);const add=()=>{if(!text.trim())return;setPosts(p=>[['Você',text,'agora'],...p] as any);setText('')};return <main className="min-h-screen bg-[#0A0A0A] text-white px-4 py-6 md:px-10"><div className="mx-auto max-w-3xl"><Link href="/aluno" className="inline-flex items-center gap-2 text-xs text-[#777] hover:text-white"><ArrowLeft size={15}/>Painel</Link><header className="mt-8"><div className="flex items-center gap-3"><Users className="text-[#FF6B00]"/><div><p className="text-[10px] uppercase tracking-[.2em] text-[#666]">Ecossistema</p><h1 className="mt-1 text-3xl font-black">Comunidade</h1></div></div><p className="mt-2 text-sm text-[#777]">Compartilhe execução, aprendizados e progresso.</p></header><div className="mt-8 rounded-2xl border border-[#262626] bg-[#151515] p-4"><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="O que você executou hoje?" className="min-h-24 w-full resize-none bg-transparent text-sm outline-none placeholder:text-[#555]"/><div className="flex justify-end"><button onClick={add} className="inline-flex items-center gap-2 rounded-full bg-[#FF6B00] px-4 py-2 text-xs font-black">PUBLICAR <Send size={13}/></button></div></div><div className="mt-5 space-y-3">{postsLocal.map((p,i)=><article key={i} className="rounded-2xl border border-[#262626] bg-[#151515] p-5"><div className="flex items-center justify-between"><b className="text-sm">{p[0]}</b><span className="text-[10px] text-[#555]">{p[2]}</span></div><p className="mt-4 text-sm leading-6 text-[#aaa]">{p[1]}</p><div className="mt-4 flex gap-5 text-[10px] text-[#666]"><span className="inline-flex items-center gap-1"><Heart size={13}/> CURTIR</span><span className="inline-flex items-center gap-1"><MessageCircle size={13}/> COMENTAR</span></div></article>)}</div></div></main>}